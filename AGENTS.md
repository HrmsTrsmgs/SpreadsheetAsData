# SpreadsheetAsData 作業指示

このファイルは、Codexがこのリポジトリで常に守る作業ルールを定義する。
設計方針や今後の作業計画は、必要に応じて `docs/` 配下の文書を参照する。

## プロジェクトの目的

SpreadsheetAsDataを、第三者がビルド、テスト、利用、レビューできるC#/.NETライブラリとして整備する。

機能数を増やすことより、次を優先する。

* 利用側コードの簡潔さ
* 変更箇所の局所化
* 責務分離
* 依存関係の整理
* 低水準APIの抽象化
* 業務的な変換規則の集約
* 公開APIの理解しやすさ
* テスト可能性
* 自動テストによる振る舞いの確認
* READMEから目的、使用方法、制約を理解できること

## 基本方針

* ユーザーの指示を、文脈上もっとも自然な意味で解釈する。
* 指示された目的、主張、制約を、勝手に弱めたり強めたりしない。
* 依頼された論点や作業範囲から外れた変更や提案を追加しない。
* 不明点をもっともらしい内容で補完しない。
* 確認できた事実、推測、提案、未確認事項を区別する。
* 未確認のAPI、型、メソッド、設定項目、コマンドを創作しない。

## 既存コードの扱い

* 作業開始前に、対象コード、関連コード、既存テスト、既存設計を確認する。
* 既存コード、既存設計、既存の命名規則を優先する。
* 依頼された範囲外のコードやファイルを変更しない。
* 関係のないファイルを整形、移動、改名、削除しない。
* ユーザーが作成した未コミット変更を破棄または上書きしない。
* 明示的な理由なしに、大規模な全面書き換えを行わない。
* 必要性が確認できない抽象化、汎用化、依存関係を追加しない。
* 変更単位を小さくし、人間が確認できる差分に保つ。

## C#コード記述

* C#の明示的な記述は、設計意図、境界、制約を読む助けになる場合に残す。
* 言語仕様上の既定値、型推論で明白な型、同じ情報の重複は書かない。
* C#では、明示しなくても `private` になるメンバーに `private` を書かない。
* C#のラムダで、引数名に固有の意味を持たせない場合は `it` とする。
* `_` は未使用引数または discard に限定し、参照する値の名前として使わない。
* 拡張メソッドの `this` 引数は `self` とする。
* 値を返すだけの短いメソッド、プロパティ、インデクサーは、読みやすさを損なわない範囲で式形式を使う。
* 複数行に分ける場合も、単に次の呼び出しへ渡すだけの一時変数は作らない。
* 一時変数を、値を受けるだけの目的で不用意に増やさない。
* 添字、位置、分割結果などの機械的な値は、意味が読み取れる名前に置き換えられる場合に一時変数を使用する。
* 日本語の仕様語彙や業務概念を英語名へ置き換えることで意味が曖昧になる場合は、一時変数を増やす効果を慎重に判断する。
* テストコードでは、日本語識別子の方が仕様として読みやすい場合は日本語識別子を使用してよい。
* 標準ライブラリや言語機能は、短く書くためだけではなく、その機能が表す意味に合わせて選ぶ。
* LINQは、メソッド構文のほうが可読性が明確に高い場合を除き、クエリ構文を使う。
* 単純な集約、変換APIへの直接接続、FluentAssertionsへつなぐ検証など、メソッド構文のほうが読みやすい場合はメソッド構文を使う。
* LINQの取得演算子は、例外を出す手段ではなく、対象データに期待する件数や順序の意味を表すために選ぶ。
* `Single` は一致がちょうど1件である不変条件を表す場合に使う。
* `SingleOrDefault` は0件を通常の分岐として扱い、複数件を前提違反として扱う場合に使う。
* `First` と `FirstOrDefault` は、複数件の一意性を要求しない場合に使う。
* 標準APIが自然に投げる例外を、そのまま公開APIの仕様として扱うかどうかは、テストで固定する対象になった時点で判断する。

## 設計と公開API

* 詳細な設計方針は `docs/design.md` を参照する。
* 公開API、依存パッケージ、ビルド構成、永続化形式を変更する前に、理由と影響範囲を確認する。
* 公開APIの変更が必要な場合は、既存利用コード、テスト、サンプルへの影響を確認する。
* SDKや低水準APIの都合を、そのまま公開APIの責務にしない。
* テストのためだけに型やメンバーの公開範囲を広げない。

## XMLコメント

* XMLコメントは、IntelliSenseとAPI参照で読む人を助けるために書く。
* `public` メンバーのコメントは、ライブラリ使用者向けに書く。
* `public` メンバーでは、外部から見た役割、引数、戻り値、例外、利用時に知るべき振る舞いを説明する。
* `internal` と `private` メンバーのコメントは、コードリーディング向けに書く。
* `internal` と `private` メンバーでは、実装上の意図、保持する状態の意味、不変条件、キャッシュ、遅延生成、低水準APIの扱いを説明する。
* 名前や型を言い換えるだけのコメントは避け、コードを読む判断材料になる内容を書く。
* コメントは丁寧に書くが、実装より詳しすぎる説明や、変更時に陳腐化しやすい説明は避ける。
* 振る舞いや制約を変更した場合は、対応するXMLコメントも更新する。

## TDD

* 振る舞いを追加または変更する場合は、実装前にその振る舞いを表すテストを追加する。
* 不具合修正では、不具合を再現するテストを先に追加する。
* 追加したテストが、意図した理由で失敗することを確認する。
* Red確認では、失敗したコマンド、失敗理由、意図した失敗かどうかの判断根拠をユーザーへ報告する。
* Red確認が意図した失敗でない場合は、Green実装へ進まず、テスト、仕様、テストデータ、既存設計の確認へ戻る。
* 公開APIを追加または変更する場合は、Red段階のテストをAPI案として扱い、Green実装へ進む前にAPIの形を確認できるようにする。
* APIレビューのためにコンパイルを通す必要がある場合は、最小限の公開APIスタブを追加してよい。
* APIレビュー用スタブは、実装済みと誤解されないように `NotImplementedException` で失敗させる。
* API仕様をまとめて検討するためにGreen対象より多いテストを先に書いた場合は、保留する仕様テストに理由付きの `Skip` を付ける。
* 仕様保留の `Skip` 理由は、テスト実行フレームワークの都合ではなく、後から読んだ人がなぜ今は実行対象でないのか、いつ解除するのかを判断するために書く。
* `Skip` 属性の行を読んだだけで、なぜそのテストが今は実行対象でないのか分かるようにする。
* 共通定数化によって `Skip` 属性の行から理由が読み取りにくくなる場合は、文字列を直接書く。
* 同じ仕様群をまとめて保留する場合でも、共通化より、その場で保留理由を理解できることを優先する。
* `Skip` 理由に単に「未実装」とだけ書かない。
* Red、Green、Refactorを区別して進める。
* Greenへ進むときは、`Skip` を一つずつ外し、失敗理由と最小実装の範囲を確認してから実装する。
* Greenでは、現在有効な対象テストを通すための必要最小限の実装を行う。
* 必要最小限とは、対象テストが要求する振る舞いを実現することであり、次以降の `Skip` 済み仕様を先取りして固定することではない。
* 対象テストが要求していない分岐、例外型、エラーメッセージ、戻り値仕様を、自分で判断して追加しない。
* 自分で `throw` を書く例外は仕様として扱い、原則として先にテストで固定する。
* 低水準API、標準ライブラリ、LINQなどの呼び出し結果として自然に発生する例外まで、すべて明示仕様にする必要はない。
* Null許容警告や型検査を通すために `!` で事実を作らない。`!` は、コード上の不変条件が明確で、読み手にもその理由が追える場合に限る。
* 共通処理への実装で、まだ `Skip` している次の仕様テストまで通りそうな場合は、実装前に影響範囲を報告する。
* 最低限の実装を超えて次の仕様まで実装した場合は、必要に応じて保留状態へ戻し、戻した理由をコミット履歴に残す。
* Green確認後に、外部から観測できる振る舞いを変えずにリファクタリングする。
* 既存テストを、実装に都合よく弱めたり削除したりしない。
* テストが誤っている可能性がある場合は、勝手に変更せず報告する。
* テスト数を増やすこと自体を目的にしない。
* 一つのテストに一つのAssertだけを機械的に強制しない。
* 内部変数やprivateメソッドを直接テストするためだけに設計を歪めない。
* 「Redまで」「Greenまで」「Refactorまで」と指定された場合は、その段階で停止する。

## テスト

* テストコードのローカル変数と一時変数の詳細方針は `docs/testing-style.md` を参照する。
* 既存のテストフレームワークと命名規則を優先する。
* テスト名は、既存方針と整合する場合は日本語文にする。
* テスト名から、対象の振る舞いと期待結果が読み取れるようにする。
* 一つのテストは、レビュー時に一つの仕様として読める粒度にする。
* ローカル変数は原則として `var` を使い、型そのものを示す必要がある場合だけ具体型を明記する。
* 一時変数は、評価回数の固定、検証対象の共有、概念境界の明示、デバッグ容易性に意味がある場合に作る。
* 一度しか使わず、右辺の式がそのまま意味を表す `actual`、`result`、`expected` などの一般名変数は作らない。
* 例外検証のラムダも原則として `var` で受け、デリゲート型そのものを示す必要がある場合だけ `Action` や `Func<T>` を明記する。
* 静的型で保証できる戻り値の型を、実行時アサーションで確認しない。
* APIの形として戻り値の型を示したい場合は、`var` ではなく明示的な型の変数で受ける。
* 実装詳細ではなく、外部から観測できる振る舞いを優先してテストする。
* 単体テストと実ファイルを使用するテストを区別する。
* テストを実行順序に依存させない。
* テストを開発者固有の絶対パスに依存させない。
* テスト同士で同じ出力ファイルを共有しない。
* 一時ファイルはテストごとに分離する。
* 一時ファイルは可能な限りテスト終了後に削除する。
* カルチャー、タイムゾーン、現在時刻へ不必要に依存させない。
* 今回のユースケースに関係しないケースを、網羅性だけを目的に追加しない。

## ビルドと検証

* 作業前に対象ソリューション、プロジェクト、.NETバージョンを確認する。
* `global.json` が存在する場合は、その指定を確認する。
* 実在するソリューションまたはプロジェクトに対してビルドを実行する。
* 実在するテストプロジェクトに対してテストを実行する。
* 存在しないパス、プロジェクト名、コマンドを推測しない。
* パッケージの追加、更新、ダウングレードを、明示的な必要性なく行わない。
* コード変更後は、対象テスト、関連テスト、可能であれば全テストとビルドを実行する。
* Ruby版のテストは、Ruby側のコードまたはテストデータを変更した場合、またはC#側の変更をRuby側へ同期した場合に実行する。
* C#側だけの変更でRuby側へ反映していない場合は、Ruby版のテストを毎回実行しなくてよい。
* ビルド警告が今回の変更によるものか、既存のものか区別する。
* 警告を無条件に抑制しない。
* テスト失敗を、根拠なく今回の変更と無関係だと判断しない。
* 実行できなかった確認項目は理由を報告する。
* テスト成功だけで、設計や生成ファイルの妥当性を保証したと扱わない。

## Gitとコミット

* 作業開始前にGitの状態を確認する。
* 作業単位ごとに、意味のまとまったコミットを作成する。
* コミット前に差分を確認し、対象のビルドとテストを実行する。
* コミット時は失敗テストがない状態を保つ。
* 保留中の仕様テストを `Skip` したままコミットする場合は、理由がテストコード上で分かるようにし、報告にも含める。
* コミット前に、コミット対象ファイル、変更内容、コミットメッセージ案をユーザーへ提示する。
* ユーザーの明示的な了承を得てから、ステージングやコミットを行う。
* 一つのコミットに無関係な変更を混在させない。
* ユーザーが作成した既存の未コミット変更を、無断でコミットに含めない。
* コミットメッセージは、変更した振る舞いまたは目的が分かる内容にする。
* 自動整形による無関係な大量差分を避ける。
* 改行コードや文字コードを必要性なく変更しない。
* ビルド出力、一時ファイル、ローカル固有設定をコミットしない。
* 明示的な指示なしに、amend、rebase、reset、force push、履歴の書き換えを行わない。
* pushは、ユーザーから指示された場合に行う。

## READMEと公開準備

* READMEと公開準備の観点は `docs/roadmap.md` を参照する。
* READMEには、実装とテストから確認できる内容だけを書く。
* 未実装機能を実装済みのように書かない。
* APIや責務の変更に応じてREADMEを更新する。
* サンプルは、公開APIの意図が読み取れる最小限のものにする。
* サンプルへ本体と無関係なUIやフレームワークを導入しない。
* 実在顧客のデータ、企業秘密、個人情報を含めない。
* APIキー、秘密鍵、アクセストークン、接続文字列を含めない。
* ユーザー名やローカル固有の絶対パスをコード、ログ、READMEへ残さない。

## 作業完了時の報告

* 変更したファイル
* 変更した振る舞い
* 変更理由
* 主要な設計判断
* Red、Green、Refactorの実施内容
* 作成したコミット
* 実行したビルドとテスト
* 成功、失敗、未実行の区別
* 未解決事項
* 未確認事項
* 既知の制約
* 残存リスク
