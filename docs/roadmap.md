# SpreadsheetAsData ロードマップ

この文書は、ポートフォリオとしての初回公開版までの実装順序と、その後の拡張候補を記録します。

実装順序は、依存関係や調査結果に応じて変更する可能性があります。

## フェーズ0: C#版の再整備

- [x] ライブラリ本体を.NET 8へ更新する
- [x] テストプロジェクトを.NET 8へ更新する
- [x] テストフレームワークをxUnitへ移行する
- [x] アサーションをFluentAssertionsへ移行する
- [x] Nullable参照型を有効にする
- [x] 暗黙的usingを有効にする
- [x] EditorConfigを整備する
- [x] Visual StudioやNuGetの生成物をGit管理から除外する
- [x] XMLドキュメントを生成できるようにする
- [x] DocFXによるAPIドキュメント生成を確認する
- [x] Open XML SDKの型を公開APIから隠す作業を開始する
- [x] ビルド、テスト、整形確認を通過させる

このフェーズをもって、環境更新と既存コードの再起作業を完了とします。

## フェーズ1: セル値読み取りの整理

- [ ] 対応するセル値型を明確にする
- [ ] 空白セルの扱いを整理する
- [ ] 文字列、数値、真偽値、日付の変換規則を整理する
- [ ] 存在しない行やセルを読み取る場合の動作を決める
- [ ] Open XML SDK固有処理を内部実装へ集約する
- [ ] セル読み取りのテストケースを追加する

## フェーズ2: 定義名

- [ ] ワークブックの定義名を列挙する
- [ ] 定義名を名前で取得する
- [ ] 単一セルを参照する定義名へ対応する
- [ ] 矩形範囲を参照する定義名へ対応する
- [ ] ワークブック全体とワークシートローカルの名前の扱いを決める
- [ ] 非対応の定義名を判別できるようにする
- [ ] 定義名のテストと使用例を追加する

## フェーズ3: Excelテーブル

- [ ] ワークブック内のテーブルを列挙する
- [ ] テーブルを名前で取得する
- [ ] テーブルの対象範囲を取得する
- [ ] テーブルの列定義を取得する
- [ ] ヘッダー名を取得する
- [ ] データ行を列挙する
- [ ] 空のテーブルを扱う
- [ ] 複数テーブルを含むワークブックを扱う
- [ ] テーブル読み取りのテストと使用例を追加する

## フェーズ4: 列名による行データの読み取り

- [ ] 行データを表す公開型を設計する
- [ ] 行から列名で値を取得できるようにする
- [ ] 列の存在確認方法を用意する
- [ ] 重複列名や空の列名に対する動作を決める
- [ ] 列順序変更に依存しない読み取りを確認する
- [ ] 行列挙のテストと使用例を追加する

想定する利用例は次のとおりです。

```csharp
foreach (var row in book.Tables["Orders"].Rows)
{
    Console.WriteLine(row["ProductName"]);
}
```

API名は設計中の例です。

## フェーズ5: 利用者定義型へのマッピング

- [ ] テーブル行を利用者定義型へ変換する
- [ ] プロパティ名と列名を対応させる
- [ ] 属性などによる列名指定方法を検討する
- [ ] 数値型の変換規則を決める
- [ ] 日付型の変換規則を決める
- [ ] nullable型の変換規則を決める
- [ ] 変換できない値に対するエラーを設計する
- [ ] 不足列と余剰列に対する動作を決める
- [ ] オブジェクトマッピングのテストと使用例を追加する

想定する利用例は次のとおりです。

```csharp
IEnumerable<Order> orders = book.Tables["Orders"].As<Order>();
```

API名は設計中の例です。

## フェーズ6: 型付き読み取りコードの生成

- [ ] `.xlsx`からテーブル定義を読み取る
- [ ] テーブル名をC#の型名またはプロパティ名へ変換する
- [ ] 列名をC#のプロパティ名へ変換する
- [ ] 列データから型候補を判定する
- [ ] null許容性を判定する
- [ ] テーブルごとの行型を生成する
- [ ] ワークブック全体を表す読み取り型を生成する
- [ ] 生成元スキーマと実ファイルの不一致を検出する
- [ ] 型名や列名の衝突を検出する
- [ ] 生成コードのテストを追加する
- [ ] 型付き読み取りのサンプルを追加する

## フェーズ7: 初回公開準備

- [ ] READMEの使用例を現行APIへ合わせる
- [ ] サンプル用`.xlsx`を用意する
- [ ] 定義名の使用例を用意する
- [ ] テーブル行の使用例を用意する
- [ ] 利用者定義型へのマッピング例を用意する
- [ ] 型付き読み取りコード生成例を用意する
- [ ] APIドキュメントを確認する
- [ ] 対応範囲と制約を確認する
- [ ] ビルド、テスト、整形確認を自動化する
- [ ] 初回公開版としてタグを付ける

## 初回公開後の候補

### 書き込み

- セル値の更新
- 行の追加
- 行の挿入
- 行の削除
- テーブル行の追加
- テーブル行の更新
- 上書き保存
- 別名保存
- 生成型を使用した書き戻し

### 大規模ファイル

- OpenXmlReaderを使用した逐次読み取り
- OpenXmlWriterを使用した逐次出力
- ファイル全体をDOMへ展開しない変換
- 差分を保持して保存時に統合する編集モデル

### Excel機能

- 書式
- 数式
- 構造化参照
- 集計行
- フィルター
- テーブルスタイル
- シートや範囲の複製

初回公開後も、Excelの全機能対応そのものは目標にしません。

SpreadsheetAsDataのデータ操作として意味のある機能から追加します。